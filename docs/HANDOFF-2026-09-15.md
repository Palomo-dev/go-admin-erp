# HANDOFF — 2026-09-15 (sesión «Database errors: timeouts and permissions»)

Documento de relevo para que otro agente continúe exactamente donde se quedó esta sesión.
Léelo entero antes de tocar nada. Todo el trabajo es sobre `main`, **sin ramas** (orden del dueño).

## 0. Reglas que el dueño ha reforzado en esta sesión

1. **Nada de ramas.** Todo en `main`. Ni siquiera si un prompt lo pide.
2. **`git commit` solo cuando él lo pida. `git push` nunca sin autorización explícita.**
3. **Migraciones: mostrarle el SQL antes de aplicarlo.** Cada migración = `.sql` en `supabase/migrations/` + rollback en `supabase/rollbacks/`. Los agentes NO aplican nada; el orquestador hace dry-run en transacción con `ROLLBACK` y luego pide permiso.
4. Nunca nombres de organizaciones cliente en código, docs, tests ni `.sql` (repo público). Usa `org <id>`.
5. Permisos en servidor, nunca por nombre de rol; el dueño prefiere `check_user_permission` (los cargos cuentan).
6. Los agentes escriben su informe en `docs/crm-revenue-os/rondas/<fase>-<rol>-rN.md` y devuelven ≤12 líneas. El orquestador anexa a `docs/crm-revenue-os/PROGRESS.md` (nunca reescribe).
7. El árbol de trabajo tiene **cientos de archivos sin commitear de varias sesiones**. Staging siempre selectivo.
8. Otra sesión activa («CRM módulo completo con llamadas, correos y WhatsApp») lleva **F10–F13 y las fases aprobadas** del CRM. Esta sesión lleva **F0-DB, F0-SEC, F0-JOBS, F0-REG**. No lanzar agentes sobre fases de la otra sesión; no editar rutas de `api/crm/{roi,demos,proposals,contracts,payments,webhooks/stripe,webhooks/documenso,health,onboarding,renewals,partners,referrals,commissions,sales-targets,seller-dashboard}/**` ni sus servicios.

## 1. Ciclo /loop del CRM — estado de las fases que lleva esta sesión

Metodología: `.devin/workflows/loop.md` (builder → tester → qa-reviewer; aprobado ≥ 9,5; tras 3 rondas < 9,5 escalar al dueño).

| Fase | Estado | Ronda | Calificación | Qué sigue |
|---|---|---|---|---|
| **F0-DB** | **APROBADA** | 4 | QA **9,5** | Aplicar (con permiso) f00_36 → f00_37 → limpieza y pasar `get_advisors`. Para el 10: versionar `20260909052947` (F9), `commCreditsService` (F16), test anti-credenciales en `.sql`. |
| **F0-JOBS** | en_revision | 4 | QA r3 8,2 | Builder r4 **terminado**; tester r4 **parado** al empezar (`findByClientRequestId` propaga error, `signal` antes de cada efecto, permisos sin `roleId === 5`, presupuesto). Al terminar: tester r4 → QA r4. **Decisión humana pendiente** (ver §3). |
| **F0-REG** | en_progreso | 3 | QA r2 8,9 | Builder r3 **parado a medias, sin informe** (cupo del plan en `ensureAiSettings`, `fn_ai_usage_month` con `coalesce` contra `pg_timezone_names`, rollback 39 literal). Al terminar: tester r3 → QA r3. |
| **F0-SEC A+B** | en_progreso | 3 | tester r2 6,0 | Builder r3 **parado a medias, sin informe** con H1 (crítico: `phone_number_id` numérico burla el filtro por organización en el webhook de WhatsApp), H2 (plantillas de otra org), H3 (**la migración f00_40 rompe `POST /api/ai-assistant/reportes`**: hay que corregir el camino antes de aplicar) y 8 más. Al terminar: tester r3 → QA. |
| **F0-SEC C+D** | en_progreso | 2 | — | Builder **parado a medias, sin informe**: `readOrgBody` único en `orgContext.ts`, aplicado a rutas que NO son de F10–F13; `requireOrgAdmin` sin nombres de rol; guardarraíl 5 por handler; rate limit fail-closed; `ORG_AMBIGUOUS`; `jti` en ws token. Ya rompió una vez `api/crm/roi/route.ts` (falta `import OrgContextError`); se le ordenó arreglarlo y dejar la lista de rutas ajenas en su informe. Al terminar: tester → QA. |

Informes de todas las rondas: `docs/crm-revenue-os/rondas/`. Historial completo: `docs/crm-revenue-os/PROGRESS.md` (buscar «2026-09-15» y «F0-»).

### ⚠️ Parada total — 2026-09-15 ~19:35 (hora Colombia)

El dueño ordenó detener todo. Se pararon en seco 4 agentes, **a mitad de trabajo**; el árbol quedó con sus cambios sin terminar. Estado exacto:

| Agente parado | Qué había hecho | Qué faltaba | Archivos tocados (sin commit) |
|---|---|---|---|
| Builder **F0-JOBS r4** | **Terminado** antes de la parada (informe `rondas/F0-JOBS-builder-r4.md`, 166/166, guardrails 76/76). | Tester r4 (parado al empezar el dry-run de la migración 42) → QA r4. | `src/lib/jobs/**` (runner, scheduler, handlers whatsapp/email/transcribe, tests `builderR4`, `testerR3*`), `jobsService`. Migración `20260915234000_crm_v4_f00_42_*` + rollback. |
| Builder **F0-REG r3** | Cupo del plan en `ensureAiSettings`, `fn_ai_usage_month` con `coalesce` de zona horaria (migración 39 editada), reintento 22023. | Estaba escribiendo los tests de `aiUsageStats` (22023) y de rutas; **sin informe** `rondas/F0-REG-builder-r3.md`. | `src/lib/services/crm/aiCostService.ts`, `supabase/migrations/20260915221000_crm_v4_f00_39_*` (+ rollback), posiblemente `src/app/api/crm/config/**`. |
| Builder **F0-SEC-AB r3** | H1/H2/H3 en código: `webhookAuthorization.ts` por `change` y string, `whatsappCloudService.ts` (`applyTemplateStatusUpdate` ahora con 4 args), `webhook/route.ts`, `ai-assistant/reportes/route.ts` con cliente de sesión + `__tests__`. | Estaba añadiendo tests de los huecos 5+ y corriendo la suite `security`; **sin informe** `rondas/F0-SEC-AB-builder-r3.md`. **Deuda visible:** `src/lib/services/crm/whatsapp/__tests__/webhookTemplateStatus.test.ts:28` llama con 3 args (tsc TS2554) — actualizar ese test. | `src/lib/security/{secrets,wsSessionToken,rateLimit}.ts` y tests, `src/lib/services/integrations/whatsapp/**`, `src/app/api/integrations/whatsapp/webhook/**`, `src/app/api/ai-assistant/reportes/**`. |
| Builder **F0-SEC-CD** | `readOrgBody` en `orgContext.ts`, `orgAdmin.ts` sin nombres de rol, `organizationBody.ts` delegando, `rateLimitStore.ts` (nuevo) + tests, guardarraíl 5 por handler en `guardrails.test.ts`, `ORG_AMBIGUOUS`. Arregló el `import OrgContextError` de `roi/route.ts` que rompió. | Estaba escribiendo los tests unitarios de `orgContext` (header/cookie discrepantes, admin-o-permiso) y `orgAdmin`; **sin informe** `rondas/F0-SEC-CD-builder-r2.md`, así que la lista de rutas ajenas (F10–F13) que deben adoptar `readOrgBody` NO está escrita: hay que derivarla con grep. | `src/lib/utils/{orgContext,orgAdmin}.ts`, `src/lib/security/{organizationBody,rateLimit,rateLimitStore}.ts`, `src/__tests__/guardrails.test.ts`, `src/app/api/crm/roi/templates/**`. |

`tsc` completo al parar: **6 errores**, de los cuales uno es de esta obra (`webhookTemplateStatus.test.ts:28`); los otros 5 son ajenos (`FormularioEdicionProducto.tsx:972-973` implicit any — ya commiteado en `e817d175`, corregir —, `f13Round3Tester.test.ts` de la otra sesión, `deliveryIntegrationService.ts:661`).

**Cómo retomar (en este orden):** (1) correr `npx jest src/lib/security src/lib/utils src/lib/jobs src/app/api/integrations/whatsapp src/app/api/ai-assistant src/__tests__/guardrails.test.ts` en `TZ=UTC` y `TZ=America/Bogota` y `tsc`, y arreglar lo rojo; (2) relanzar los tres builders sin informe en modo «inspeccionar y completar» para que escriban su informe; (3) tester + QA de cada uno; (4) recién entonces mostrar al dueño las migraciones 38, 39, 40 (solo tras verificar `reportes`), 41 y 42.

### Agentes en vuelo al momento del relevo (histórico, antes de la parada)
Si retomas en una sesión nueva, esos subagentes ya no existen: **inspecciona el árbol** (`git status`, informes en `rondas/`) y relanza «en modo inspeccionar y completar», nunca rehacer:
- Builder F0-JOBS r4 → verifica `src/lib/jobs/**`, `outboundService.ts`, `src/lib/jobs/__tests__/testerR3*.test.ts` verdes en `TZ=UTC` y `TZ=America/Bogota`.
- Builder F0-REG r3 → `src/lib/services/crm/aiCostService.ts`, `ensureAiSettings`, migración 39 editada, `f0RegTester.r2*.test.ts` verdes.
- Builder F0-SEC-AB r3 → `src/lib/services/integrations/whatsapp/webhookAuthorization.ts`, webhook, `src/app/api/ai-assistant/reportes`.
- Builder F0-SEC-CD → `src/lib/utils/orgContext.ts` (`readOrgBody`), `orgAdmin.ts`, `guardrails.test.ts` caso 5, `rateLimit.ts`. **Comprobar que `tsc` no tenga cientos de errores por el refactor a medias** (`NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json`).

## 2. Migraciones PENDIENTES DE APLICAR (ninguna aplicada; todas con rollback)

Orden sugerido y dependencias. Antes de cada una: dry-run `begin; …; rollback;` por MCP y mostrar el SQL al dueño.

| Archivo | Fase | Notas |
|---|---|---|
| `20260915230000_crm_v4_f00_36_ai_usage_logs_rls_y_rec_initplan.sql` | F0-DB | Cierra `ai_usage_logs`/`comm_usage_logs` a `anon`, políticas por pertenencia, `rec_*` con `(select auth.uid())`. Dry-run OK (tester r3). |
| `20260915231000_crm_v4_f00_37_can_contact_guarda_y_release_job_tope.sql` | F0-DB | Guarda en `fn_can_contact`, `fn_release_job` con backoff y `dead`. Dry-run OK. |
| `20260915232000_crm_v4_f00_limpieza_jobs_huerfanos.sql` | F0-DB | DML: borra 6 jobs `crm_event` huérfanos (org 125). Rollback «no restaura datos». |
| `20260915220000_crm_v4_f00_38_ai_settings_solo_rpc.sql` | F0-REG | Créditos solo por RPC (grants por columna). Dry-run OK (tester r2). |
| `20260915221000_crm_v4_f00_39_decrement_ai_credits_guardas.sql` | F0-REG | Guardas NaN/negativo/sin fila; `fn_ai_usage_month`. **Se está editando en r3** (coalesce de zona horaria). |
| `20260915223000_crm_v4_f00_40_cerrar_rpc_crm_anon.sql` | F0-SEC-B | 32 RPC. Dry-run OK. **NO aplicar hasta que SEC-AB r3 corrija `/api/ai-assistant/reportes` (H3).** |
| `20260915233000_crm_v4_f00_41_pg_cron_alineado_con_vercel.sql` | F0-JOBS | Job 17 → `*/2`, job 19 con 4 kinds; no activa nada. |
| `20260915234000_crm_v4_f00_42_idx_messages_client_request_id.sql` | F0-JOBS | Índice parcial sobre `messages.metadata->>'client_request_id'` (258 875 filas). Decisión del dueño. |
| `crm_v4_f00_43_*` (RPC única del cupo del plan) | F0-REG r3 | Nueva. Rollback md5 verificado. |
| `crm_v4_f00_44_*` (trigger de validación de zona horaria) | F0-REG r3 | Nueva. |
| `20260916000000_crm_v4_f0sec_rate_limit_buckets.sql` | F0-SEC-CD | Buckets del rate limit (RPC atómica). Orden: aplicar → `RATE_LIMIT_STORE=db` → deploy. |
| `20260915210000_f11_eliminar_mv_customer_health.sql` | F11 (otra sesión) | Borra `refresh_mv_customer_health()` y la MV. Aplicar solo tras desplegar código sin lectores. |

Migración ya aplicada hoy por esta sesión (con permiso): `20260915203000_website_live_visitors_hora_servidor.sql`.

## 3. Decisiones pendientes del dueño

1. **Permiso para ver/reintentar la cola de jobs (F0-JOBS):** (a) roles admin/manager (`STAGE_MANAGER_ROLE_IDS`) o cualquier cargo con `admin.full_access` — es lo provisional que implementa r4 —, o (b) crear permiso `crm.jobs.view` asignable a cargos (requiere fila en `permissions`).
2. Aplicar el índice `crm_v4_f00_42`.
3. Autorizar el lote de migraciones del §2.
4. Commits: hay **cuatro bloques de esta sesión sin commitear** (ver §5) y el trabajo del ciclo CRM (el de F10–F13 ya lo commiteó la otra sesión en `47fbc8c5` y `fd9c2bf2`).

## 4. Desktop (Electron) — roadmap `docs/ROADMAP-DESKTOP.md`

- **Fase 0** hecha por otra sesión (en el árbol, sin commit).
- **Fase 1 (instalador) — HECHA y commiteada en `main`: `3d3cdf62`.** `artifactName` con versión, `perMachine: false` + `asInvoker`, detección/desinstalación de la 0.1.0 per-machine (`customInit`), macros `customPageAfterChangeDir/customInstall/customUnInstall`, icono desde el `.exe`, `publish` a `Palomo-dev/go-admin-desktop-releases`, firma por env (`CSC_*` nativo; Azure Trusted Signing vía `electron-builder.config.js` + `build/sign-azure.js`), workflow `.github/workflows/desktop-release.yml`. Contradicciones con la auditoría anotadas en el mensaje de cierre: la 0.1.2 NO salió per-machine (fue la 0.1.0); el repo de código es público. **Pendiente del dueño:** crear el repo público de releases con un commit, secret `DESKTOP_RELEASES_TOKEN`, trámite del certificado.
- **Fase 2 (UI nativa)** — no empezada. Prompt en `docs/ROADMAP-DESKTOP.md` §Fase 2. Toca `mainWindow.ts`/`index.ts`, que tienen cambios de fase 0 sin commitear.
- **Fase 3 (Next embebido)** — no empezada. Prompt en el roadmap §Fase 3.
- Fase 4 — no empezar.

## 5. Bloques de esta sesión — YA COMMITEADOS en `main` (2026-09-15 ~19:20)

`fda2f93d` inicio · `a804f0fb` invitaciones · `8579a510` visitantes en vivo · `e817d175` aviso a la tienda · `2bf8f45d` impresión + fase 0 desktop · `d3f250c1` F0-DB (56 migraciones reconstruidas + f00_36/f00_37/limpieza pendientes + docs + este handoff). Tienda (`goadmin-websites`): `5c0a461` por la otra sesión. **Nada pusheado.** Sin commitear: lo de la tabla «Parada total» y `guardrails.test.ts` (en obra de SEC-CD).

Detalle de cada bloque (para contexto):

1. **Dashboard de inicio** (`src/app/app/inicio/page.tsx`, `DashboardAlertas.tsx`, guardrails): un solo skeleton hasta resolver el rol (otra sesión refinó `rolResuelto` con `resolvedOrganizationId`); módulos una vez por org; secciones en paralelo.
2. **Caché del catálogo de la tienda** (repo `C:\Users\USUARIO\goadmin-websites`: `lib/supabase/cache.ts` `cacheCatalog` TTL 30 s por org, `queries.ts` con `PRODUCT_LIST_COLUMNS`, `app/api/revalidate`) + **ERP**: `src/lib/services/website/{revalidarCatalogoWeb,avisarCambioCatalogo}.ts`, `src/app/api/website/revalidate`, hooks en acciones masivas / crear / editar producto / ajustes; `.env.example` `WEBSITES_BASE_URLS`. Motivo: caída real de Postgres el 14-sep por ráfaga en la tienda de la org 145 (memoria `incidente-2026-09-14-caida-bd-storefront`).
3. **Invitaciones**: `src/lib/auth/cuentaInvitacion.ts` (nueva/huérfana/existente decidido en servidor), `GET /api/auth/invite/context`, `accept-invitation` endurecido (antes cambiaba la contraseña de cuentas existentes con solo el código), `InvitationWizard` con flujo «ya tienes cuenta → inicia sesión», salida con `window.location.assign`.
4. **Visitantes en vivo**: `useLiveVisitors.ts` sobre la RPC `website_live_visitors` (hora del servidor, sesiones distintas). Migración ya aplicada. Ojo: **el reloj del PC del dueño está ~9 h atrasado**; le avisé que sincronice Windows.
5. **Impresión** (analizado, parcialmente implementado): sesión propia del agente por código de vinculación (`POST /api/desktop/agent-session` + `startAgentWithTokenHash` en Electron, versión 0.1.3), Bluetooth por spooler RAW (`print-agent/src/printerDrivers.ts`, `PrinterFormDialog` con `/bluetooth`). Pendientes 3–6 del análisis: `print_jobs` a Realtime (migración), estación obligatoria + aviso en POS, estado por `last_seen_at`, vinculación por código en `print-agent` de consola. Memoria: `impresion-agente-hallazgos`.

## 6. Prompt listo para pegar en la sesión nueva

```
Trabaja en C:\Users\USUARIO\CascadeProjects\go-admin-erp sobre main, sin ramas. Lee primero
docs/HANDOFF-2026-09-15.md entero y luego .devin/workflows/loop.md y CLAUDE.md.
Retoma el ciclo /loop del CRM SOLO para F0-JOBS, F0-REG, F0-SEC (A+B y C+D); F0-DB ya está
aprobada. Otra sesión lleva F10–F13: no toques sus rutas ni lances agentes sobre esas fases.
Para cada fase: inspecciona qué dejó el último builder (rondas/ y git status), completa sin
rehacer, y sigue builder → tester → qa-reviewer con subagentes hasta ≥ 9,5, anexando a
docs/crm-revenue-os/PROGRESS.md. Migraciones: nunca las apliques; dry-run en transacción con
rollback y muéstrame el SQL. Antes de nada, dime si hay errores de tsc por el refactor de
readOrgBody y arréglalos. Cuando F0 esté cerrada, sigue con la fase 2 del desktop
(docs/ROADMAP-DESKTOP.md) sobre main.
```

---

## Cierre 2026-09-16 (madrugada) — estado final de esta sesión

**Todo lo pedido está hecho y en `main`.**

| Bloque | Estado | Dónde |
|---|---|---|
| F0-DB | APROBADA 9,5 | migraciones 36, 37, limpieza aplicadas el 16 |
| F0-SEC (A+B y C+D) | APROBADA 9,5 | `rondas/F0-SEC-AB-qa-r4.md`, `F0-SEC-CD-qa-r3.md`; deudas A y B cerradas; migraciones 40 y `rate_limit_buckets` aplicadas |
| F0-JOBS | APROBADA 9,6 | `rondas/F0-JOBS-qa-r5.md` + `builder-r5b`; permisos `crm.jobs.view` ∧ `crm.jobs.retry` (migración 45 aplicada); 37/41/42 aplicadas |
| F0-REG | APROBADA 9,6 | `rondas/F0-REG-qa-r4.md`; migraciones 38/39/43/44 aplicadas (39 con `fn_resolve_timezone`) |
| Desktop fase 1 (instalador/firma) | hecha | commit `3d3cdf62` |
| Desktop fase 2 (UI nativa) | hecha | `docs/desktop/FASE-2-UI-NATIVA.md`, en `5c4150f4` |
| Desktop fase 3 (Next embebido offline) | hecha y probada sin red | `docs/desktop/FASE-3-NEXT-EMBEBIDO.md`, commit `bbba57a2` |
| Impresión local en Desktop (fase 4.1) | hecha | `docs/desktop/IMPRESION-LOCAL-DESKTOP.md`, en `5c4150f4` |

**Pendientes que NO dependen de código:**
1. Vercel: `RATE_LIMIT_STORE=db` (la migración ya está; hasta entonces el rate limit es por instancia y lo avisa en logs).
2. `git push` del commit `bbba57a2` (fase 3) — la sesión «CRM módulo completo…» está haciendo los push de `main`; si no, pedir autorización al dueño.
3. Fase 3, criterio restante: prueba sin red con sesión iniciada y datos cacheados (falta un usuario de prueba con datos; el dueño ofreció `servicio@goadmin.io`: iniciar sesión a mano en el Desktop, cortar la red y comprobar POS/inventario cacheados).
4. Deuda F0-SEC (C): sobrecarga síncrona de `readOrgBody` no mira la query (37 rutas); `f0RegTesterR4.test.ts:235` sigue `it.failing`.
5. Desktop fase 4 (salvo 4.1 ya hecha): NO empezar sin orden del dueño.
6. Migraciones de POS (`pos_terminals`, `pos_display_feedback`) son de la sesión POS: sin aplicar, fuera de este alcance.

**Reglas aprendidas hoy (para cualquier agente):** nunca `git stash`/`apply` del árbol completo mientras haya testers de mutación (una mutación viva viajó a un stash ajeno); no matar procesos `node` por nombre; un tester de mutación por zona, copia en el scratchpad, md5 árbol↔copia por ruta tras cada mutación.

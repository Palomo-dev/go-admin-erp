# F-43 · tax_account_mapping es tabla muerta y apunta a cuentas inexistentes

**Severidad:** Baja (tabla sin uso)
**Estado:** Documentado: tabla marcada OBSOLETA en su comentario; no se borra (2026-09-23)
**Bloque:** E (deuda técnica)

## Hallazgo

La tabla `tax_account_mapping` tiene 288 filas en 48 organizaciones, TODAS con
`organization_tax_id IS NULL`. Mapea a `account_code` de 8 dígitos:
`23170501`, `23650501`, `23650502`, `24080501`, `24080502`, `24080503`.

Verificado: esos códigos existen en CERO organizaciones de `chart_of_accounts`.
Verificado: NINGUNA de las 43 funciones `fn_auto_journal_*` referencia la tabla.

## Implicación

No se debe usar `tax_account_mapping` en el arreglo de F-42. No se borra
todavía — se registra como tabla muerta para limpieza futura.

## Cifras

- 288 filas
- 48 organizaciones
- 288 con `organization_tax_id IS NULL` (100%)
- 0 códigos de 8 dígitos existen en `chart_of_accounts`

## Estado al 2026-09-23 (cierre del núcleo contable)

`COMMENT ON TABLE tax_account_mapping` = «OBSOLETA…» (migración
`20260923081953`). 0 referencias en `pg_proc`. Propuesta: eliminarla en una
limpieza de esquema cuando se confirme que ningún reporte externo la lee; las
cuentas de impuesto viven en `accounting_rules.tax_account_code`, y poblarla
crearía una segunda fuente de verdad.

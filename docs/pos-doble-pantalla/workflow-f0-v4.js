export const meta = {
  name: 'pos-doble-pantalla-f0-v4',
  description: 'Fase 0 v4: ronda 4 dirigida de B, C y D con el feedback de la ronda 3 y alcance congelado, luego integración',
  phases: [
    { title: 'Parte A', detail: 'aprobada en v3 (9,7): se salta' },
    { title: 'Partes B C D', detail: 'emisión, ruta /pos-display, indicador y configuración' },
    { title: 'Integración', detail: 'pruebas de extremo a extremo y QA final' },
  ],
}

const PLAN = args.plan
const DATE = args.date
const UMBRAL = 9.5
const MAX_RONDAS = 3

const REGLAS = `
Reglas del repositorio que aplican a tu trabajo (además del CLAUDE.md que ya tienes):
- Español de Colombia en código, comentarios y textos. Nunca escribas el nombre de una organización cliente.
- No crees archivos .sql ni migraciones en esta fase: la Fase 0 no toca la base de datos.
- Deja limpios de ESLint los archivos que toques (npx eslint <archivo>). Los ~190 errores de tsc preexistentes en OTROS archivos no son tuyos.
- Nunca uses toISOString().split('T')[0] ni .split('T')[0] sobre valores de la BD.
- No edites PROGRESS.md ni ${PLAN}: los actualiza el orquestador.
- No hagas git commit ni git push.
- Trabaja en el árbol de trabajo tal cual está (hay cambios sin commitear de otras sesiones en otros archivos: no los toques ni los reviertas).
`

const CONTEXTO = `
Contexto verificado (no lo re-descubras, úsalo):
- Plan completo en ${PLAN}. Léelo entero antes de empezar; tu parte está definida ahí (§8 protocolo, §4 UX, §5 cajero, §12 Fase 0).
- Carrito del POS: tipos en src/components/pos/types.ts (Cart, CartItem, CartItemModifier). Servicio: src/lib/services/posService.ts (clase POSService con métodos estáticos; el carrito se guarda en localStorage con saveCartsToStorage / saveCartToStorage, clave pos_carts_<orgId>). NO cambies cómo se guarda.
- Página del POS: src/app/app/pos/page.tsx (usa CartView, CartTabs, CheckoutDialog). Modal de cobro: src/components/pos/CheckoutDialog.tsx.
- Precedente de pantalla propia fuera de /app: src/app/qr-display/[deviceId]/page.tsx.
- Configuración del POS: src/components/pos/configuracion/ConfiguracionPage.tsx (tarjetas + modales en ConfigModals.tsx). Persiste ajustes en la tabla organization_settings con upsert({organization_id, key, settings, updated_at}, {onConflict:'organization_id,key'}); claves existentes: pos_categories_display, pos_blind_cash_count, pos_cash_session_mode, pos_require_cash_session, operating_hours. La clave nueva es pos_customer_display.
- Marca: organizations.logo_url, primary_color, secondary_color, timezone. Hook de organización: useOrganization() en @/lib/hooks/useOrganization.
- Tests: jest con testEnvironment node, testMatch **/__tests__/**/*.test.ts (solo .ts, no .tsx). Node ≥ 18 tiene BroadcastChannel global: sirve para probar el transporte sin navegador.
- i18n con next-intl (useTranslations, useLocale). Formato de moneda: busca el helper que usa el recibo/CartView y reutilízalo.
`

const BUILD_SCHEMA = {
  type: 'object',
  properties: {
    resumen: { type: 'string' },
    archivos: { type: 'array', items: { type: 'string' } },
    feedbackAtendido: { type: 'array', items: { type: 'string' } },
    decisiones: { type: 'array', items: { type: 'string' } },
    pendientes: { type: 'array', items: { type: 'string' } },
  },
  required: ['resumen', 'archivos', 'feedbackAtendido', 'decisiones', 'pendientes'],
}

const TEST_SCHEMA = {
  type: 'object',
  properties: {
    ejecutados: { type: 'number' },
    pasaron: { type: 'number' },
    fallaron: { type: 'number' },
    fallos: { type: 'array', items: { type: 'object', properties: {
      severidad: { type: 'string', enum: ['crítico', 'alto', 'medio', 'bajo'] },
      descripcion: { type: 'string' },
      reproducir: { type: 'string' },
      esperado: { type: 'string' },
      obtenido: { type: 'string' },
    }, required: ['severidad', 'descripcion', 'reproducir', 'esperado', 'obtenido'] } },
    noProbado: { type: 'array', items: { type: 'string' } },
    robustez: { type: 'number' },
    evidencia: { type: 'string' },
    testsAgregados: { type: 'array', items: { type: 'string' } },
  },
  required: ['ejecutados', 'pasaron', 'fallaron', 'fallos', 'noProbado', 'robustez', 'evidencia', 'testsAgregados'],
}

const QA_SCHEMA = {
  type: 'object',
  properties: {
    calificacion: { type: 'number' },
    fortalezas: { type: 'array', items: { type: 'string' } },
    problemas: { type: 'array', items: { type: 'object', properties: {
      severidad: { type: 'string', enum: ['crítico', 'alto', 'medio', 'bajo'] },
      descripcion: { type: 'string' },
      accion: { type: 'string' },
    }, required: ['severidad', 'descripcion', 'accion'] } },
    paraElDiez: { type: 'array', items: { type: 'string' } },
    veredicto: { type: 'string', enum: ['aprobado', 'requiere-nueva-ronda'] },
  },
  required: ['calificacion', 'fortalezas', 'problemas', 'paraElDiez', 'veredicto'],
}

const PARTES = {
  A: {
    nombre: 'A · Protocolo, proyección del carrito y transporte',
    alcance: `
Crea src/lib/pos/display/ con:
1. protocol.ts — los tipos de §8 del plan (DownMessage, UpMessage, DisplayState, DisplayCart, DisplayPayment) con versión v: 1. Exporta también un type guard isDownMessage/isUpMessage que valide forma y versión.
2. projection.ts — projectCartForDisplay(cart: Cart, opts: { currency: string; lastChangedLineId?: string | null }): DisplayCart. Sin el objeto product completo. Nombres de modificadores y extraPrice. Descuentos por línea y totales, etiqueta de descuento si hay cupón/promoción en el carrito (mira qué campos tiene Cart para eso; si no hay, deja discountLabel null y documenta). Respeta tax_included. Debe ser función pura y determinista.
3. transport.ts — interfaz DisplayTransport (§3.2) y BroadcastChannelTransport: canal por terminalId ('pos-display:<terminalId>'), seq creciente por emisor, descarte de mensajes de otra terminal o con seq no creciente, y heartbeat opcional (start/stop) cada 1 s. Debe funcionar en Node ≥ 18 (BroadcastChannel global) para poder probarse.
4. terminal.ts — getOrCreateLocalTerminalId(): identidad local en localStorage ('pos_terminal_id'), UUID generado una vez. En Fase 0 no hay tabla; documenta que F2 la formaliza.
5. Tests en src/__tests__/pos-display/: proyección (varios carritos: vacío, con modificadores, descuento, impuesto incluido/excluido, 200 líneas), type guards, y transporte (dos canales en el mismo proceso: emisor y receptor; descarte por terminal ajena y por seq viejo; heartbeat). Deben pasar con npx jest src/__tests__/pos-display.
Criterios: cero any innecesario, exports con nombre, comentarios breves donde haya decisión no obvia.`,
  },
  B: {
    nombre: 'B · Emisión desde posService y CheckoutDialog',
    alcance: `
1. Un emisor único src/lib/pos/display/emitter.ts: mantiene el DisplayState actual, expone setCart(cart|null), setPayment(payment|null), setMode(...), y publica por el transporte de Parte A de forma COALESCIDA (requestAnimationFrame en navegador, setTimeout 0 en tests) para que varias mutaciones en la misma vuelta emitan un solo 'state'. Emite 'hello' al arrancar y responde 'need_snapshot' con hello + state completo. Respeta el interruptor maestro: lee organization_settings/pos_customer_display.enabled (la Parte D lo persiste; aquí solo léelo con un helper cacheado en memoria y una función refresh()); si está apagado, no emite nada.
2. Conecta el emisor en el POS: tras CADA mutación del carrito activo en src/lib/services/posService.ts (addItem, remove, updateQuantity, descuentos, hold, clear, etc.) llama al emisor. Hazlo en un único punto (donde se guarda el carrito) para no olvidar rutas. No cambies cómo se guarda el carrito.
3. CheckoutDialog.tsx: al abrir el cobro, setPayment según el método: cash → total/received/change en vivo mientras el cajero teclea; card/bold_card → method card; cualquier método QR (nequi, daviplata, breb_qr, bold_qr, bancolombia_qr, redeban_qr, qr, transfer) → method 'qr' con qr: null (la imagen llega en F2). Al confirmar la venta → mode 'thanks' con total, y a los 8 s → idle. Al cancelar → vuelve a 'order'.
4. Tests en src/__tests__/pos-display/emitter.test.ts: coalescencia (3 setCart seguidos = 1 state), hello al inicio, respuesta a need_snapshot, y que con enabled=false no se emite. Mockea el transporte.
Regla: no importes nada de React en emitter.ts (debe ser probable en Node).`,
  },
  C: {
    nombre: 'C · Ruta /pos-display',
    alcance: `
1. src/app/pos-display/page.tsx ('use client', fuera de /app: sin sidebar ni header) + componentes en src/components/pos-display/. Usa el transporte de Parte A: al montar envía need_snapshot con capabilities {touch: navigator.maxTouchPoints > 0, width, height} y pinta el DisplayState que reciba.
2. Estados de §4.2 para esta fase: Reposo (marca + reloj), Pedido (§4.3: líneas con resaltado de 600 ms en lastChangedLineId, subtotal, descuento con etiqueta, impuestos como 'IVA incluido' o desglose según taxIncluded, TOTAL grande), Cobro·efectivo (total, recibido, cambio), Cobro·tarjeta (total + 'Siga las instrucciones del datáfono'), Cobro·QR sin imagen (total + 'Siga las instrucciones del cajero'), Gracias (8 s), Conectando (sin heartbeat 3 s: oculta importes), Cerrado no aplica aún.
3. Marca: logo, primary_color y nombre de la organización desde useOrganization(); si el color de marca no alcanza contraste AA sobre blanco, oscurécelo (helper pequeño). Tipografías fluidas con clamp(): total ≥ 96 px en 1920×1080, legible en 1024×768.
4. Sin scroll: si no caben las líneas, muestra las últimas N y 'y X más'.
5. Fundido de 200 ms entre estados; sin animaciones > 300 ms; sin sonidos.
6. Botón discreto de pantalla completa (requestFullscreen) que se oculta si window.electronAPI existe.
7. i18n con next-intl: añade las claves necesarias en TODOS los archivos de mensajes que use el proyecto (busca dónde viven, p. ej. messages/es.json y en.json) y no dejes textos hardcodeados.
8. Moneda con el mismo helper que el POS. Zona horaria de la organización para el reloj (usa lo de src/lib/utils/dateDisplay.ts; nunca hardcodees America/Bogota).
Pruebas que debes dejar: un test .ts de la lógica pura que extraigas (p. ej. el helper de contraste y el recorte de líneas) en src/__tests__/pos-display/.`,
  },
  D: {
    nombre: 'D · Indicador en el POS y tarjeta en Configuración › POS',
    alcance: `
1. Indicador en la cabecera de src/app/app/pos/page.tsx: punto verde 'Pantalla del cliente conectada' / gris 'Sin pantalla' según si el emisor de Parte B ha recibido need_snapshot/heartbeat de una pantalla en los últimos 3 s (expón lo necesario desde el emisor sin acoplar React a él). Al pulsarlo: menú con 'Abrir pantalla del cliente' y 'Cerrar'.
2. Abrir en web: window.open('/pos-display', 'pos-display', 'popup,width=1280,height=800'); nombre fijo para no duplicar. Primera vez: aviso (toast) 'Arrastre la ventana a la pantalla del cliente y pulse F11', recordado en localStorage. Si existe window.electronAPI?.posDisplay?.open úsalo (llegará en F1); si no, el window.open.
3. Tarjeta 'Pantalla del cliente' en src/components/pos/configuracion/ConfiguracionPage.tsx con el MISMO patrón que las demás tarjetas (tarjeta + modal en ConfigModals.tsx). Contenido de Fase 0: interruptor maestro 'Pantalla del cliente' y botón 'Abrir ahora'. Persiste en organization_settings con key 'pos_customer_display' y settings {enabled: boolean} usando exactamente el mismo upsert/onConflict que operating_hours. Lee el valor al cargar. Al guardar, llama al refresh() del emisor (Parte B) para que la caja aplique el cambio sin recargar.
4. El módulo de propina/calificación NO va en esta fase: deja un comentario indicando que llega en F2.
5. i18n: claves en los archivos de mensajes del proyecto, sin textos hardcodeados.
Deja limpios los archivos que toques.`,
  },
}

const NOTA_CIERRE_A = 'IMPORTANTE: la Parte A YA EXISTE en src/lib/pos/display/ con 11 suites de tests en verde. Esta es una RONDA DE CIERRE: lee lo que hay, atiende la lista de feedback, y no reescribas lo que funciona. Ejecuta npx jest src/__tests__/pos-display al final: todo debe seguir en verde.'

const FEEDBACK_R3 = {
  B: "1. [qa \u00b7 alto] El override de totales de TaxSummary queda OBSOLETO cuando cambian las l\u00edneas y se emite igualmente. Reproducido con una sonda en Node: carrito con 1 l\u00ednea y `setTotals('c1', {total: 5950})` \u2192 flush \u2192 `onCartsSaved([carrito con 2 l\u00edneas, subtotal 12500, total 14875])` \u2192 flush \u21d2 el state publicado lleva 2 l\u00edneas, `subtotal: 12500` y `total: 5950`. Causa: `DisplayEmitter.setCart` (emitter.ts:421-424) solo descarta `totalsOverride` si el carrito queda sin l\u00edneas o cambia de id, no cuando `!sameLines(prev, next)`; y `project()` (emitter.ts:531-537) aplica el override mientras coincida el id. En la app el override se corrige solo cuando TaxSummary termina su efecto (TaxSummary.tsx:106-190), que hace `await POSService.getProductTaxes(item.product_id)` SECUENCIAL por cada \u00edtem (dos consultas a Supabase cada uno, posService.ts:2497-2515): con 8 l\u00edneas son ~16 viajes de red. Resultado: tras CADA l\u00ednea a\u00f1adida o cantidad cambiada, el cliente ve durante cientos de ms (m\u00e1s de 1 s en carritos medianos) las l\u00edneas nuevas con el TOTAL de la venta anterior, es decir, un total que no suma con lo que tiene delante (PLAN \u00a74.1 \u00abNunca miente\u00bb, \u00a74.3 \u00abnunca una cifra que el recibo no vaya a repetir\u00bb). Es el flujo principal del POS, no un borde.\n   Acci\u00f3n: En `DisplayEmitter.setCart` (emitter.ts), tras calcular `equivalent`: si `!equivalent && this.totalsOverride && next && this.totalsOverride.cartId === next.id`, poner `this.totalsOverride = null` y reproyectar (`next = this.project()`) ANTES de asignar `projectedCart`, de modo que el frame use los totales del propio `Cart` (que posService acaba de recalcular con `calculateCartTotals`, coherentes con las l\u00edneas) hasta que TaxSummary reenv\u00ede por `setTotals`. A\u00f1adir en emitter.test.ts el caso exacto de la sonda (setTotals \u2192 onCartsSaved con una l\u00ednea m\u00e1s \u2192 flush \u21d2 `state.cart.total === 14875`, nunca 5950) y el inverso (setTotals posterior vuelve a aplicar el override). Documentar en la cabecera de emitter.ts que el override caduca con cada mutaci\u00f3n de l\u00edneas.\n2. [qa \u00b7 medio] Un carrito en `status: 'cancelled'` con l\u00edneas se proyecta como \u00abPedido\u00bb (confirmado en c\u00f3digo, coincide con el tester). Flujo real: CartView.handleCancelDebt (CartView.tsx:493-496) \u2192 `POSService.cancelDebtWithCreditNote` pone `cart.status = 'cancelled'` y llama a `saveCartsToStorage(allCarts)` (posService.ts:2923-2928) \u2192 `onCartsSaved` proyecta; luego `onCartUpdate(result.cart)` lo mantiene como pesta\u00f1a activa (page.tsx:308-319 `updateCartInState` no lo retira) \u2192 `setActiveCart(cancelado)`. `project()` (emitter.ts:534) solo devuelve null para `hold_with_debt`, as\u00ed que la pantalla pasa de \u00abReposo\u00bb (deuda) a un \u00abPedido\u00bb con las l\u00edneas y el total de una venta ANULADA. El propio posService ya define qu\u00e9 es un carrito vivo: `getActiveCarts` filtra `status === 'active' || status === 'hold'` (posService.ts:823); el emisor usa una lista negra en vez de esa lista blanca.\n   Acci\u00f3n: En `DisplayEmitter.project()` sustituir `if (this.cart.status === 'hold_with_debt') return null;` por la lista blanca `if (this.cart.status !== 'active' && this.cart.status !== 'hold') return null;` (mismo criterio que `POSService.getActiveCarts`, posService.ts:823) y actualizar el comentario de cabecera (\u00abModo resultante\u00bb) para decir que solo `active`/`hold` se proyectan. Pasar el `it.failing` de tester-r10-parte-b.test.ts:161 a `it` y a\u00f1adir en emitter.test.ts un caso con `status: 'completed'` y otro con `'cancelled'` \u21d2 mode idle, cart null.\n3. [qa \u00b7 bajo] \u00abGracias\u00bb tras \u00abGuardar con deuda\u00bb muestra `cart.total` (CartView.tsx:316) pero la factura a cr\u00e9dito se emite con `taxCalculation.finalTotal` recalculado por `calculateCartTaxesComplete` (posService.ts:1244-1267 `total: taxCalculation.finalTotal`), y el toast de la misma funci\u00f3n ya muestra `result.invoice.total` (CartView.tsx:309). Con l\u00edneas `tax_excluded` u override de `organization_taxes` (justo los casos por los que existe `setTotals`) la pantalla dice un total pagado que el recibo no repite (PLAN \u00a74.3).\n   Acci\u00f3n: En CartView.handleHoldWithDebt cambiar a `getPosDisplayEmitter().setMode('thanks', { total: Number(result.invoice.total) || cart.total })` (el emisor ya cae al total proyectado si el n\u00famero no es finito). Tipar `invoice.total` en el retorno de `holdCartWithDebt` si sigue siendo `any`, o al menos anotar el `Number()` en el comentario.\n4. [qa \u00b7 bajo] El interruptor maestro solo se consulta en `start()`/`refresh()`; `flush()` y `announce()` publican sin comprobar `isEnabled()`. El comentario de settings.ts:15-16 (\u00abla caja consulta `enabled` en cada emisi\u00f3n (tras cada tecla)\u00bb) describe algo que el emisor NO hace. Hoy lo mitiga el cableado (configuracionService \u2192 `primeCustomerDisplaySettings` + `applyPosDisplaySettings()`; otras ventanas \u2192 evento `storage` \u2192 `refreshPosDisplay`), pero cualquier ruta futura que escriba la cach\u00e9 sin avisar (o `clearCustomerDisplaySettingsCache()` al cerrar sesi\u00f3n con la caja arrancada) deja la caja emitiendo con el interruptor apagado. Test documental del tester en tester-r10-parte-b.test.ts:307 lo demuestra.\n   Acci\u00f3n: Opci\u00f3n A (preferida, barata): en `DisplayEmitter.flush()` y `announce()` a\u00f1adir al inicio `if (!this.isEnabled()) { this.cancelPending(); this.closeTransport(); return; }` y convertir el test documental de tester-r10:307 en afirmaci\u00f3n de que NO se publica. Opci\u00f3n B: corregir el comentario de settings.ts para decir que la lectura ocurre solo en `start`/`refresh` y que quien escriba la cach\u00e9 debe llamar a `applyPosDisplaySettings()`. Elegir una y dejarlo escrito en la cabecera de emitter.ts.\nT1. [tester \u00b7 medio] Un carrito en status 'cancelled' con l\u00edneas se proyecta como \u00abPedido\u00bb. Flujo real: CartView.handleCancelDebt \u2192 POSService.cancelDebtWithCreditNote pone cart.status = 'cancelled' (posService.ts:2923) y guarda la lista \u2192 onCartsSaved; luego onCartUpdate(result.cart) lo mantiene como pesta\u00f1a activa \u2192 setActiveCart(cancelado). DisplayEmitter.project() (emitter.ts) solo devuelve null para 'hold_with_debt', as\u00ed que la venta a cr\u00e9dito ANULADA reaparece en la pantalla del cliente como un pedido pendiente con sus l\u00edneas y su total (viola PLAN \u00a74.1 \u00abNunca miente\u00bb). Antes de anular, ese mismo carrito (hold_with_debt) estaba en reposo, as\u00ed que el cliente ve pasar la pantalla de \u00abBienvenido\u00bb a un \u00abPedido\u00bb de una venta que ya no existe.\n   Reproducir: Test it.failing en src/__tests__/pos-display/tester-r10-parte-b.test.ts (\u00abAnular deuda deja el carrito en status cancelled\u2026\u00bb): start \u2192 setActiveCart(hold_with_debt con 2\u00d7Caf\u00e9) \u2192 flush \u2192 mode idle; onCartsSaved([{...mismo carrito, status:'cancelled'}]) + setActiveCart(clon) \u2192 flush. Al pasarlo a it: Expected 'idle', Received 'order'. En la app: POS \u2192 carrito con l\u00edneas \u2192 \u00abGuardar con deuda\u00bb \u2192 (pesta\u00f1a sigue activa) \u2192 bot\u00f3n \u00abAnular deuda\u00bb (CartView.tsx:1031) \u2192 mirar /pos-display.\n   Esperado: Un carrito 'cancelled' (y en general cualquier status que no sea active/hold) no se proyecta: reposo con cart null, igual que hold_with_debt. | Obtenido: state.mode === 'order' con las l\u00edneas y el total de la venta anulada.\nT2. [tester \u00b7 bajo] \u00abGracias\u00bb tras \u00abGuardar con deuda\u00bb usa cart.total (CartView.tsx:316: setMode('thanks', { total: cart.total })), pero la factura a cr\u00e9dito se emite con taxCalculation.finalTotal recalculado por calculateCartTaxesComplete (posService.holdCartWithDebt, l\u00edneas ~1244-1267: invoice.total = taxCalculation.finalTotal). El propio CartView muestra en el toast result.invoice.total. Cuando ambos difieren (l\u00edneas con tax_excluded, override de organization_taxes: justo los casos por los que existe el override de totales de la Parte B), la pantalla dice un total pagado que el recibo no repite (PLAN \u00a74.3).\n   Reproducir: An\u00e1lisis est\u00e1tico: comparar CartView.tsx:305-316 (toast con result.invoice.total vs setMode con cart.total) y posService.ts holdCartWithDebt (total: taxCalculation.finalTotal). Caso pr\u00e1ctico: carrito con una l\u00ednea \u00abExcluir impuesto\u00bb y otra con IVA, guardar con deuda, comparar el total de \u00abGracias\u00bb con el de la factura.\n   Esperado: setMode('thanks', { total: Number(result.invoice.total) || cart.total }) (o el total que TaxSummary envi\u00f3 por setTotals), para que \u00abGracias\u00bb repita la cifra de la factura. | Obtenido: \u00abGracias\u00bb muestra cart.total, que puede diferir del total facturado.\nT3. [tester \u00b7 bajo] El interruptor maestro solo se lee en start()/refresh(): si la cach\u00e9 de settings.ts pasa a enabled=false sin que nadie llame a refresh(), flush()/announce() siguen publicando. Hoy est\u00e1 mitigado por el cableado (configuracionService \u2192 primeCustomerDisplaySettings + applyPosDisplaySettings(); otras ventanas \u2192 evento storage \u2192 refreshPosDisplay), pero el comentario de settings.ts (\u00abla caja consulta enabled en cada emisi\u00f3n\u00bb) no describe lo que hace el emisor, y una futura ruta que escriba la cach\u00e9 sin avisar dejar\u00eda la caja emitiendo con el interruptor apagado.\n   Reproducir: Test documental en tester-r10-parte-b.test.ts (\u00absi la cach\u00e9 pasa a enabled=false y nadie llama a refresh()\u2026\u00bb): start con enabled=true \u2192 enabled=false \u2192 setActiveCart(con l\u00edneas) \u2192 flush \u21d2 isEmitting true y state 'order' publicado; solo refresh() lo apaga.\n   Esperado: Comprobaci\u00f3n barata de isEnabled() en flush()/announce() (o corregir el comentario de settings.ts para que documente que la lectura es solo en refresh). | Obtenido: Con enabled=false en cach\u00e9 y sin refresh(), la caja publica un state con el carrito.\nPara el 10 (opcional si cabe): Resolver los cuatro problemas anteriores con sus tests (el del override obsoleto es el que m\u00e1s pesa: es el camino principal y el cliente lo ve en cada tecla). | Ejecutar `npx next build` en la ronda de cierre: ni builder ni tester lo corrieron y el PLAN \u00a712 lo exige para cerrar la fase. | Cubrir con un test la secuencia real de cobro con la l\u00ednea temporal del navegador: `checkout` (removeCart \u2192 onCartsSaved(null)) seguido de `setMode('thanks')` en la misma vuelta \u21d2 un solo state 'thanks', nunca un 'payment' con `cart: null` intermedio. Hoy se cumple por c\u00f3mo encadena las microtareas `POSService.checkout`, pero nada lo protege si alguien mete un `await` real entre `removeCart` y el `return`.",
  C: "1. [qa \u00b7 medio] Moneda equivocada al abrir o recargar la pantalla durante \u00abGracias\u00bb (o en un cobro cuyo carrito qued\u00f3 sin l\u00edneas). Confirmado en c\u00f3digo: emitter.ts:590 construye thanks con cart: null; sanitizeDisplayCart (logic.ts:441) devuelve null para un carrito sin l\u00edneas y con \u00e9l se pierde currency; CustomerDisplay.tsx:92-94 arranca currencyRef en FALLBACK_CURRENCY ('COP') y lo muta durante el render. Una organizaci\u00f3n en USD/EUR ve \u00ab$ 11,90\u00bb en formato COP durante hasta 8 s. Contradice PLAN \u00a74.1.3 (\u00abnunca miente\u00bb) y la aceptaci\u00f3n de F0 \u00abrecargar la pantalla recupera la venta completa\u00bb. La causa es que la moneda solo viaja dentro del carrito.\n   Acci\u00f3n: Llevar la moneda en el hello, que siempre precede al snapshot (announce): (1) protocol.ts ~l\u00ednea 201: a\u00f1adir `currency?: string` opcional al draft/mensaje hello (aditivo; isDownMessage en protocol.ts:351-356 solo exige organizationId/sessionOpen/cashier, as\u00ed que no rechaza el campo); (2) emitter.ts:604-610 helloDraft() a\u00f1ade `currency: this.currency`; (3) useDisplayReceiver.ts: DisplayHello gana `currency: string | null` y el case 'hello' lo guarda; (4) sustituir currencyRef por una funci\u00f3n pura en logic.ts `resolveDisplayCurrency({ cartCurrency, helloCurrency, remembered }): string` con prioridad carrito \u2192 hello \u2192 \u00faltima recordada \u2192 FALLBACK_CURRENCY, y que CustomerDisplay la use con un useRef solo para `remembered` actualizado en un useEffect (no en el render); (5) tests en customer-display-logic.test.ts para las 4 prioridades y pasar a `it` los dos `it.failing` de tester-r3-parte-c.test.ts:138-155, reescribiendo currencyAsCustomerDisplaySees (l\u00edneas 108-115) para que consuma resolveDisplayCurrency con el hello en vez de replicar la l\u00f3gica vieja.\n2. [qa \u00b7 medio] Reposo falso con caja viva pero sin estado. Confirmado en c\u00f3digo: tras bye o silencio, forgetCashier() deja state = null y el receptor de la Parte A adopta a la siguiente instancia que hable aunque sea solo un heartbeat (transport.ts:602-607); entonces evaluateHealth marca alive, resolveView(logic.ts:311) devuelve 'idle' porque `if (!state) return 'idle'`, y como ya est\u00e1 \u00abviva\u00bb deja de pedir need_snapshot (useDisplayReceiver.ts:180). El cliente ve reloj + \u00abBienvenido\u00bb con un pedido en curso hasta la siguiente mutaci\u00f3n o hasta que el cajero enfoque la pesta\u00f1a. El mismo `!state \u2192 idle` produce adem\u00e1s un fotograma de Reposo entre el hello y el state de cada announce (llegan como dos eventos de mensaje distintos, React los pinta por separado y `key={view}` remonta el fundido dos veces).\n   Acci\u00f3n: (1) logic.ts resolveView: con `connected` y `state === null` devolver 'connecting' (no se sabe qu\u00e9 hay en la caja: PLAN \u00a74.1.3), y a\u00f1adir el caso a los tests de resolveView y a tester-r3-parte-c \u203a \u00abadopci\u00f3n por latido tras bye\u00bb (que hoy fija la precondici\u00f3n). (2) useDisplayReceiver.ts evaluateHealth: tras el bloque `if (!alive) {...}` a\u00f1adir `if (alive && previousStateRef.current === null && now - lastSnapshotAt >= RESNAPSHOT_INTERVAL_MS) askSnapshot();` para que una instancia adoptada por latido reciba un need_snapshot cada 2 s hasta que conteste con state. (3) Test .ts con BroadcastChannel en src/__tests__/pos-display/: A hello+state \u2192 A bye \u2192 B solo heartbeats 1/s durante 5 s \u2192 se cuentan \u2265 2 need_snapshot en el canal y la vista resuelta nunca es 'idle'; y el caso hello-sin-state \u2192 'connecting'.\n3. [qa \u00b7 bajo] La compuerta `npx jest` sigue siendo intermitente por tester-r8-parte-b.test.ts:725-741 (\u00abstop() de la caja manda bye\u2026 Gracias vence y vuelve a idle\u00bb): usa tick(20)/tick(40) fijos y en mis 4 corridas de la carpeta fall\u00f3 1 (Expected 'thanks', Received 'order'). El builder solo pas\u00f3 a untilReceived el test del need_snapshot; quedan tick(30)/tick(40) en las l\u00edneas 264, 402, 635-640, 681, 710, 730-738.\n   Acci\u00f3n: En tester-r8-parte-b.test.ts sustituir las esperas fijas por untilReceived (ya existe en la l\u00ednea 45): tras setMode('thanks') esperar `ms.at(-1)?.t === 'state' && ms.at(-1).state.mode === 'thanks'`; luego esperar a que el \u00faltimo state sea 'idle' (con tope \u2265 thanksDurationMs del e2e() + 500 ms); tras stop() esperar `ms.at(-1)?.t === 'bye'`. Aplicar el mismo patr\u00f3n a las otras 7 esperas fijas del archivo. Verificar con 5 corridas seguidas de `npx jest src/__tests__/pos-display` todas verdes.\n4. [qa \u00b7 bajo] Error de tipos en tester-r3-parte-c.test.ts:251 con `npx tsc --noEmit`: `views` se infiere como string[] y `views.map(viewShowsAmounts)` no compila (TS2345). ts-jest no lo detecta, pero rompe la compuerta tsc que el PLAN \u00a712 exige para cerrar la fase.\n   Acci\u00f3n: En tester-r3-parte-c.test.ts, declarar `const views: DisplayView[] = []` (importar DisplayView de '@/components/pos-display/logic') y comprobar con `npx tsc --noEmit -p tsconfig.json` (NODE_OPTIONS=--max-old-space-size=8192) que no aparece ning\u00fan error en src/__tests__/pos-display.\n5. [qa \u00b7 bajo] El layout ra\u00edz pinta UI encima del cliente en /pos-display: PWAInstallPrompt.tsx (fixed bottom-4 z-[100], texto \u00abInstalar GoAdmin ERP\u00bb hardcodeado en espa\u00f1ol, aparece en Chrome de escritorio cuando la app es instalable y no se descart\u00f3 en 7 d\u00edas), PushNotificationManager.tsx (con sesi\u00f3n y permiso 'default' lanza el di\u00e1logo nativo de notificaciones 3 s despu\u00e9s de montar) y el Toaster. Ninguno filtra por ruta. Contradice PLAN \u00a74.1.4 (\u00abcero navegaci\u00f3n, no hay men\u00fas\u00bb) y \u00a74.1.5 (marca del comercio, no de GO Admin). Mitigado en parte porque comparten origen y estado con la pesta\u00f1a de la caja.\n   Acci\u00f3n: A\u00f1adir en src/lib/pos/display/ (o en logic.ts) `isCustomerDisplayPath(pathname: string | null): boolean` (true para '/pos-display' y '/pos-display/*') con test .ts, y usarla en PWAInstallPrompt.tsx y PushNotificationManager.tsx para devolver null / no pedir permiso cuando `usePathname()` (nullable, ver memoria tsc del repo) cae en la pantalla del cliente, con comentario que cite PLAN \u00a74.1.4. El Toaster puede quedarse si se documenta que solo emite avisos de sesi\u00f3n bajo /app.\nT1. [tester \u00b7 medio] Moneda equivocada al recargar la pantalla durante \u00abGracias\u00bb o durante un cobro cuyo carrito no tiene l\u00edneas. El protocolo no lleva `currency` en `thanks` ni en `payment` (el emisor manda `thanks` con `cart: null`, emitter.ts buildState), `sanitizeDisplayCart` descarta un carrito sin l\u00edneas junto con su `currency`, y `CustomerDisplay.currencyRef` arranca en FALLBACK_CURRENCY ('COP'). Una organizaci\u00f3n en USD/EUR/MXN que recargue la pantalla (o la abra) en esa ventana ve el total con formato COP. Contradice PLAN \u00a74.1.3 (\u00abnunca miente\u00bb) y la aceptaci\u00f3n de F0 \u00abrecargar la pantalla recupera la venta completa\u00bb. Ra\u00edz en Parte A/B (protocolo sin moneda), s\u00edntoma en Parte C.\n   Reproducir: 1) Abrir /pos-display en un navegador con `pos_terminal_id` en localStorage. 2) Desde la consola de la propia p\u00e1gina: `const ch=new BroadcastChannel('pos-display:'+localStorage.getItem('pos_terminal_id')); const s=(m,seq)=>ch.postMessage({...m,v:1,seq,terminalId:localStorage.getItem('pos_terminal_id'),instanceId:'dddddddd-0000-4000-8000-00000000000d'});` 3) `s({t:'hello',organizationId:999999,cashier:null,sessionOpen:true},0); s({t:'state',state:{mode:'thanks',cart:null,payment:null,tip:null,thanks:{total:11.9,askRating:false}}},1)`. 4) Leer `document.body.innerText`. Variante: `s({t:'state',state:{mode:'payment',cart:{id:'x',currency:'USD',lines:[],subtotal:0,discountTotal:0,discountLabel:null,taxTotal:0,taxIncluded:false,total:0,lastChangedLineId:null},payment:{method:'card',total:11.9,provider:null},tip:null,thanks:null}},2)`. Test: src/__tests__/pos-display/tester-r3-parte-c.test.ts \u203a \u00abmoneda \u00b7 pantalla reci\u00e9n abierta\u2026\u00bb (2 `it.failing`).\n   Esperado: \u00abGracias por su compra | Total pagado | US$ 11,90\u00bb y \u00abPAGO CON TARJETA | TOTAL | US$ 11,90\u00bb (la moneda de la organizaci\u00f3n; en la misma sesi\u00f3n de pantalla, tras un pedido en USD, s\u00ed sale US$). | Obtenido: \u00abGracias por su compra | Total pagado | $ 11,90\u00bb y \u00abPAGO CON TARJETA | TOTAL | $ 11,90\u00bb (formato COP) observado en el navegador a 1024\u00d7768 tras recargar la pantalla.\nT2. [tester \u00b7 bajo] Reposo falso tras adopci\u00f3n por latido: despu\u00e9s de un `bye` (o del silencio del watchdog) `useDisplayReceiver` olvida el estado (forgetCashier) y el receptor adopta a la primera instancia que hable aunque solo sea un `heartbeat`; `lastReceivedAt` avanza, `evaluateHealth` marca conectado con `state === null` \u2192 vista Reposo, y como ya est\u00e1 \u00abviva\u00bb deja de repetir `need_snapshot`. Si esa instancia (otra pesta\u00f1a del POS, o la misma caja tras un silencio > 3 s sin cambiar de foco) tiene un pedido o un cobro en curso, la pantalla muestra Reposo hasta la siguiente mutaci\u00f3n del carrito o hasta que el cajero enfoque la pesta\u00f1a (reannounce en /app/pos). Mitigado por el reannounce al foco, por eso bajo.\n   Reproducir: 1) /pos-display abierta. 2) Consola de la p\u00e1gina: crear canal como en el fallo 1 con instancia A, mandar hello+state(order con 1 l\u00ednea) y ver `data-view=order`. 3) Mandar `{t:'bye'}` \u2192 `data-view=connecting`. 4) Con otra instancia B (`instanceId:'bbbbbbbb-0000-4000-8000-00000000000b'`) mandar SOLO `{t:'heartbeat',at:Date.now()}` cada 1 s, sin hello ni state. 5) Esperar 5 s y contar los `need_snapshot` que llegan al canal. Precondici\u00f3n fijada en tester-r3-parte-c \u203a \u00abreceptor \u00b7 adopci\u00f3n por latido tras bye\u00bb.\n   Esperado: Mientras haya caja viva pero ning\u00fan `state` aceptado, la pantalla deber\u00eda seguir en Conectando o volver a pedir `need_snapshot` (a la instancia adoptada) hasta recibir un estado; nunca Reposo con datos desconocidos. | Obtenido: `data-view` pasa a `idle` (Reposo con reloj y \u00abBienvenido\u00bb) en \u2264 1,6 s y en 5 s se env\u00edan 0 `need_snapshot` (observado en el navegador: `needSnapshotsToB: 0`, `after5s: 'idle'`).\nT3. [tester \u00b7 bajo] Test inestable en la suite (Parte B, no C): tester-r8-parte-b \u203a \u00abstop() de la caja manda bye y la pantalla lo recibe; \u00abGracias\u00bb vence y vuelve a idle\u2026\u00bb usa ventanas fijas `tick(20)`/`tick(40)` y fall\u00f3 en la corrida completa de la carpeta bajo carga (Expected 'thanks', Received 'order'), aunque pasa 3/3 aislado. El builder arregl\u00f3 solo el test del need_snapshot con `untilReceived`; quedan 15 `tick(n)` fijos en el archivo. Rompe la compuerta `npx jest` de forma intermitente.\n   Reproducir: `npx jest src/__tests__/pos-display src/__tests__/guardrails.test.ts` (corrida completa, con las 21+ suites en paralelo). Fall\u00f3 en el primer intento de esta ronda en src/__tests__/pos-display/tester-r8-parte-b.test.ts:734; `npx jest src/__tests__/pos-display/tester-r8-parte-b.test.ts` \u00d73 \u2192 verde.\n   Esperado: Verde de forma determinista: esperar con sondeo (`untilReceived`) al state coalescido con mode 'thanks' antes de asertar, como ya hace el test del need_snapshot. | Obtenido: 1 de 3 corridas completas roja: `expect(x.states().at(-1)!.mode).toBe('thanks')` \u2192 'order'.\nPara el 10 (opcional si cabe): Medir en 1366\u00d7768 (PLAN \u00a713 la exige junto a 1024\u00d7768 y 1920\u00d71080) y anotar en el comentario de SCALE_STYLE cu\u00e1ntas filas caben. | Verificar con una sesi\u00f3n real la marca (logo externo, primary_color pasando por ensureAaOnWhite) y el bot\u00f3n de pantalla completa oculto bajo window.electronAPI en la app de escritorio; hoy ambos solo tienen cobertura unitaria. | Al relevar instancia (dos pesta\u00f1as de /app/pos), previousStateRef conserva el carrito de la instancia anterior y shouldHighlightLine resalta por `previous.id !== next.id`; decidir expl\u00edcitamente si un relevo debe resaltar y fijarlo con test.",
  D: "1. [qa \u00b7 medio] refreshCustomerDisplaySettings (src/lib/pos/display/settings.ts:136-152) no descarta respuestas superadas: cada evento storage lanza una consulta independiente y en la l\u00ednea 141 `cache.set(orgId, settings)` escribe la \u00daLTIMA RESPUESTA en llegar, no el \u00faltimo guardado. Reproducido: dos relecturas (fila true, luego fila false) resueltas en orden inverso dejan la cach\u00e9 en enabled:true y el emisor abierto con la BD en false. Efecto visible al cliente: la caja emite con el interruptor apagado (o al rev\u00e9s) hasta el siguiente evento o recarga. La afirmaci\u00f3n del builder \u00abel \u00faltimo estado guardado manda\u00bb solo se cumple si las respuestas llegan en orden.\n   Acci\u00f3n: En settings.ts a\u00f1adir `const epoch = new Map<number, number>()` y una funci\u00f3n `bump(orgId)` que incremente y devuelva el valor. En refreshCustomerDisplaySettings: `const mine = bump(orgId)` ANTES del await de fetchCustomerDisplaySettings, y tras el await `if (epoch.get(orgId) !== mine) return cache.get(orgId) ?? settings;` antes de `cache.set`. primeCustomerDisplaySettings tambi\u00e9n llama a bump(orgId) tras su cache.set. Convertir el it.failing 'encender y apagar seguidos\u2026' de src/__tests__/pos-display/tester-r3-parte-d.test.ts:166 en `it` y hacer que su hermano 'conducta actual documentada' (l\u00ednea 193) exija enabled:false e isEmitting false. Verificar: `npx jest src/__tests__/pos-display/tester-r3-parte-d.test.ts -t 'fuera de orden'` verde sin it.failing.\n2. [qa \u00b7 bajo] loadCustomerDisplaySettings (settings.ts:117-120) hace `cache.set(orgId, settings)` incondicional al resolverse, aunque un primeCustomerDisplaySettings haya fijado la cach\u00e9 mientras la consulta estaba en vuelo. Reproducido: load lenta (fila ausente) + prime(true) + resoluci\u00f3n de la load \u2192 cach\u00e9 enabled:false. Secuencia real en SPA: entrar a /app/pos con red lenta, ir a Configuraci\u00f3n \u203a POS y encender, volver al POS: startPosDisplay usa la cach\u00e9 envenenada (no consulta porque hay cach\u00e9) y la caja arranca apagada con la BD en true.\n   Acci\u00f3n: Misma epoch del punto anterior: en loadCustomerDisplaySettings capturar `const mine = bump(orgId)` antes de crear `load`, y tras `const settings = await load` hacer `if (epoch.get(orgId) === mine) cache.set(orgId, settings); return cache.get(orgId) ?? settings;`. Adem\u00e1s primeCustomerDisplaySettings debe hacer `inflight.delete(orgId)` para que un load posterior no reciba la promesa vieja. Convertir el it.failing 'se enciende en Configuraci\u00f3n mientras la carga del POS sigue en vuelo\u2026' (tester-r3-parte-d.test.ts:235) en `it` y actualizar su hermano (l\u00ednea 265) para exigir isEmitting true y db.reads === 2. Comprobar que posDisplay.test.ts (Parte B) y posDisplay-settings-channel.test.ts siguen verdes: `npx jest src/__tests__/pos-display`.\n3. [qa \u00b7 bajo] CustomerDisplayIndicator.tsx pinta la etiqueta indicator.notEmitting (\u00abDesactivada en Configuraci\u00f3n \u203a POS \u203a Pantalla del cliente\u00bb) siempre que `emitting` es false, pero emitting tambi\u00e9n es false (a) mientras startPosDisplay a\u00fan carga el interruptor (primeros cientos de ms, m\u00e1s con red lenta) y (b) cuando isBroadcastChannelSupported() es false (createBrowserTransport devuelve null en posDisplay.ts:49). En ambos casos el aviso manda al cajero a Configuraci\u00f3n a por un interruptor que ya est\u00e1 encendido.\n   Acci\u00f3n: Exponer desde settings.ts `hasCustomerDisplaySettingsCache(orgId): boolean` (cache.has) y reexportar isBroadcastChannelSupported desde posDisplay.ts o index.ts. En presence.ts ampliar DisplayPresenceSnapshot con `reason: 'loading' | 'unsupported' | 'disabled' | null` (calculado en readDisplayPresence a partir de un tercer par\u00e1metro o de dos getters nuevos del emisor) y en el indicador: reason 'loading' \u2192 sin etiqueta; 'unsupported' \u2192 nueva clave i18n indicator.unsupported en es/en/fr/pt; 'disabled' \u2192 la etiqueta actual. A\u00f1adir dos casos en presence.test.ts (cargando y sin BroadcastChannel) y mantener isSamePresence comparando tambi\u00e9n reason.\n4. [qa \u00b7 bajo] posCustomerDisplay.toast.closeFromOpener en messages/{es,en,fr,pt}.json dice \u00abci\u00e9rrela en su ventana (Alt+F4)\u00bb y \u00abpulse \u00abAbrir\u00bb para recuperarla\u00bb. Alt+F4 es de Windows (el \u00fanico target de electron-builder.yml, pero la web se usa tambi\u00e9n en macOS, donde no hace nada). Y \u00abAbrir\u00bb solo recupera la ventana si la abri\u00f3 ESTA misma pesta\u00f1a antes de recargar (mismo grupo de contextos); si la abri\u00f3 otra pesta\u00f1a, window.open crea una SEGUNDA /pos-display y la primera queda hu\u00e9rfana. El propio openDisplay.ts (cabecera, l\u00edneas 24-29) documenta esa limitaci\u00f3n, pero el texto la promete como salida universal.\n   Acci\u00f3n: Cambiar el texto en los 4 idiomas a algo sin atajo de SO y sin prometer recuperaci\u00f3n: p. ej. es: \u00abNo se puede cerrar desde aqu\u00ed: ci\u00e9rrela desde su propia ventana. Si esta caja la abri\u00f3 antes de recargar, \u00abAbrir\u00bb la recupera.\u00bb Mantener la clave. Actualizar la comprobaci\u00f3n est\u00e1tica de tester-r2-parte-d.test.ts que exige que el texto cite la primera palabra de menu.open (sigue cumpli\u00e9ndose) y a\u00f1adir una que rechace 'Alt+F4' en los 4 idiomas.\nT1. [tester \u00b7 medio] refreshCustomerDisplaySettings (src/lib/pos/display/settings.ts) no descarta respuestas superadas: cada evento `storage` lanza una consulta independiente y la \u00daLTIMA RESPUESTA en llegar escribe la cach\u00e9, no el \u00faltimo guardado. Con dos guardados seguidos en Configuraci\u00f3n (encender, apagar) y la primera respuesta llegando despu\u00e9s de la segunda (HTTP/2 multiplexado, reintento, latencia variable de Supabase), la caja queda emitiendo con la BD en `enabled:false` (o apagada con la BD en true) hasta el siguiente evento o recarga. El builder afirma que 'el \u00faltimo estado guardado mande', pero solo es cierto si las respuestas llegan en orden.\n   Reproducir: npx jest src/__tests__/pos-display/tester-r3-parte-d.test.ts -t 'fuera de orden'. El caso `it.failing` 'encender y apagar seguidos\u2026' reproduce: prime(120,{enabled:false}); startPosDisplay; fire(storage) con fila true (lectura #0 retenida); fire(storage) con fila false (lectura #1 retenida); resolver #1 y luego #0. El caso hermano 'conducta actual documentada' fija la conducta err\u00f3nea: getCachedCustomerDisplaySettings(120).enabled === true y emitter.isEmitting === true con la BD en false.\n   Esperado: Tras responder las dos relecturas, la cach\u00e9 refleja la fila m\u00e1s reciente de la BD (enabled:false) y el emisor cierra el transporte. Sugerencia: contador de petici\u00f3n por organizaci\u00f3n en refreshCustomerDisplaySettings y descartar la respuesta si su n\u00famero es menor que el de la \u00faltima petici\u00f3n emitida (o deduplicar relecturas en vuelo y relanzar una al terminar si hubo marca nueva). | Obtenido: La respuesta vieja (true) pisa la nueva (false): cach\u00e9 enabled:true, emitter.isEmitting true. Aserci\u00f3n que falla: tester-r3-parte-d.test.ts:189 `expect(getCachedCustomerDisplaySettings(120).enabled).toBe(false)` \u2192 Received: true.\nT2. [tester \u00b7 bajo] loadCustomerDisplaySettings (settings.ts) escribe la cach\u00e9 incondicionalmente al resolverse, aunque la promesa haya sobrevivido a stopPosDisplay y otro c\u00f3digo haya fijado la cach\u00e9 mientras tanto. Secuencia SPA real: el cajero entra a /app/pos (consulta lenta en vuelo, fila ausente), sale a Configuraci\u00f3n \u203a POS, enciende el interruptor (upsert OK + primeCustomerDisplaySettings(true)), y la consulta vieja responde 'sin fila' \u2192 cache.set(false). Al volver al POS, startPosDisplay usa la cach\u00e9 envenenada (no consulta) y la caja arranca APAGADA con la BD en true. Solo lo corrige recargar o un evento storage de otra ventana. Es la misma clase de carrera que el qa se\u00f1al\u00f3 para refresh (que s\u00ed protege el prime en su rama de error) pero en la carga inicial y en la rama de \u00e9xito.\n   Reproducir: npx jest src/__tests__/pos-display/tester-r3-parte-d.test.ts -t 'carga inicial lenta'. `it.failing` 'se enciende en Configuraci\u00f3n mientras la carga del POS sigue en vuelo\u2026' + hermano 'conducta actual documentada': hold=true; startPosDisplay (lectura #0 retenida); stopPosDisplay; ConfiguracionService.saveCustomerDisplayConfig({enabled:true}) (lectura #1 liberada, upsert OK, prime true); liberar #0; startPosDisplay de nuevo \u2192 isEmitting false y db.reads === 2 (no reconsult\u00f3).\n   Esperado: Un prime posterior a la petici\u00f3n en vuelo prevalece: la cach\u00e9 queda en true y la caja arranca encendida al volver al POS sin consultar. Sugerencia: en loadCustomerDisplaySettings, tras el await, `if (!cache.has(orgId)) cache.set(...)` (o un epoch que prime incremente y load compare), y que primeCustomerDisplaySettings borre/inutilice el inflight de esa organizaci\u00f3n. | Obtenido: cache.set(orgId, {enabled:false}) pisa el prime; getCachedCustomerDisplaySettings(120).enabled === false (aserci\u00f3n tester-r3-parte-d.test.ts:258 \u2192 Received: false) y el emisor arranca sin transporte.\nT3. [tester \u00b7 bajo] El men\u00fa del indicador (CustomerDisplayIndicator.tsx) muestra 'Desactivada en Configuraci\u00f3n \u203a POS \u203a Pantalla del cliente' siempre que `emitting` es false, pero `emitting` tambi\u00e9n es false (a) mientras startPosDisplay a\u00fan carga el interruptor (primeros cientos de ms tras montar, o m\u00e1s con red lenta) y (b) cuando el entorno no soporta BroadcastChannel (isBroadcastChannelSupported() false: Safari < 15.4, WebViews antiguos). En (a) y (b) el aviso es falso y manda al cajero a Configuraci\u00f3n a por un interruptor que ya est\u00e1 encendido.\n   Reproducir: An\u00e1lisis est\u00e1tico: useCustomerDisplayPresence lee emitter.isEmitting (= transport !== null); posDisplay.ts createBrowserTransport devuelve null si !isBroadcastChannelSupported(); el indicador pinta indicator.notEmitting con `!emitting` sin distinguir causa. Manual: abrir /app/pos con el interruptor encendido y desplegar el men\u00fa antes de que resuelva la consulta de organization_settings (throttling 'Slow 3G' en DevTools).\n   Esperado: Distinguir 'cargando' (no mostrar la etiqueta, o 'Comprobando\u2026') y 'entorno sin soporte' (texto propio) de 'desactivada en Configuraci\u00f3n'. Basta exponer si settings.ts tiene cach\u00e9 para la organizaci\u00f3n (o si startPosDisplay termin\u00f3) y si el transporte es compatible. | Obtenido: Etiqueta 'Desactivada en Configuraci\u00f3n \u203a POS \u203a Pantalla del cliente' en los dos casos aunque pos_customer_display.enabled sea true.\nT4. [tester \u00b7 bajo] toast.closeFromOpener en es/en/fr/pt indica 'ci\u00e9rrela en su ventana (Alt+F4)'. Alt+F4 es un atajo de Windows; en macOS (navegador y Electron) el atajo es Cmd+W/Cmd+Q y Alt+F4 no hace nada. Adem\u00e1s el texto dice 'pulse \u00abAbrir\u00bb para recuperarla': window.open con nombre fijo solo alcanza la emergente si est\u00e1 en el mismo grupo de contextos (la abri\u00f3 esta misma pesta\u00f1a antes de recargar); si la abri\u00f3 OTRA pesta\u00f1a, \u00abAbrir\u00bb crea una SEGUNDA pantalla y la primera sigue hu\u00e9rfana (limitaci\u00f3n admitida por el builder, pero el texto la promete como salida universal).\n   Reproducir: Ver messages/{es,en,fr,pt}.json \u2192 posCustomerDisplay.toast.closeFromOpener. Manual: pesta\u00f1a A abre la pantalla; pesta\u00f1a B de /app/pos (mismo navegador) \u2192 men\u00fa \u2192 Cerrar \u2192 toast; pulsar Abrir en B \u2192 dos ventanas /pos-display.\n   Esperado: Texto sin atajo espec\u00edfico de SO ('ci\u00e9rrela desde su propia ventana') y sin prometer que \u00abAbrir\u00bb siempre la recupera, o condicionar la frase a que exista referencia propia perdida (misma pesta\u00f1a recargada). | Obtenido: Instrucci\u00f3n inaplicable en macOS y una salida que en el caso 'otra pesta\u00f1a' duplica la pantalla.\nPara el 10 (opcional si cabe): Cerrar las dos carreras de cach\u00e9 de settings.ts con una sola epoch por organizaci\u00f3n (prime, load y refresh la avanzan; cada escritor as\u00edncrono solo escribe si su epoch sigue vigente) y convertir los dos it.failing de tester-r3-parte-d en tests verdes normales. | Que el men\u00fa del indicador distinga \u00abcargando\u00bb, \u00abentorno sin BroadcastChannel\u00bb y \u00abdesactivada en Configuraci\u00f3n\u00bb en vez de una \u00fanica etiqueta para todo `!emitting`. | Texto de closeFromOpener sin atajo de SO y sin prometer que \u00abAbrir\u00bb siempre recupera la ventana.",
}

const ALCANCE_CONGELADO_A = `
ALCANCE CONGELADO (decisión del orquestador con autoridad del dueño): esta parte ya pasó tres rondas completas en la v3 y su alcance queda cerrado a lo construido más la lista de feedback de la ronda 3 que recibió el builder en esta ronda. Tu calificación se limita a: (a) que esa lista esté resuelta correctamente y con test, y (b) que no haya fallos críticos o altos (pérdida de datos, seguridad, ruptura de otro módulo, cifra al cliente distinta de la del cajero, build roto). Hallazgos NUEVOS de severidad media o baja fuera de la lista van a paraElDiez o a noProbado y NO bajan la nota. No escribas tests «HALLAZGO» que fallen a propósito; convierte lo que encuentres en tests del comportamiento correcto solo si es crítico/alto.`

function promptBuilder(parte, ronda, feedbackPrevio) {
  const p = PARTES[parte]
  return `Eres el BUILDER de la parte ${p.nombre} de la Fase 0 del POS de doble pantalla (ronda ${ronda}).
${CONTEXTO}
${REGLAS}
Tu alcance exacto:
${p.alcance}
${parte === 'A' ? NOTA_CIERRE_A : ''}
${feedbackPrevio ? `\nEsta es una RONDA DE CORRECCIÓN. Feedback del tester y del qa-reviewer de la ronda anterior; trátalo como lista obligatoria en orden de severidad:\n${feedbackPrevio}\n` : ''}
${parte !== 'A' ? 'Los archivos de la Parte A (src/lib/pos/display/protocol.ts, projection.ts, transport.ts, terminal.ts) ya existen y están aprobados: úsalos. Solo modifícalos si es estrictamente necesario y de forma ADITIVA (nuevos exports), y dilo en decisiones.' : ''}
Antes de terminar: corre npx eslint sobre cada archivo que tocaste y npx jest sobre los tests que creaste; corrige lo que salga. No declares la parte como lista: eso lo decide el qa-reviewer.`
}

function promptTester(parte, ronda, build) {
  const p = PARTES[parte]
  return `Eres el TESTER de la parte ${p.nombre} de la Fase 0 del POS de doble pantalla (ronda ${ronda}). Tu trabajo es romper lo construido, no defenderlo. No arregles nada: reporta con pasos reproducibles.
${CONTEXTO}
Alcance de la parte (lo que DEBERÍA cumplir):
${p.alcance}
Lo que el builder dice que hizo (verifícalo, no lo creas):
${JSON.stringify(build, null, 2)}
Qué hacer:
1. Lee los archivos cambiados y los tests. Corre npx jest src/__tests__/pos-display (y la suite existente src/__tests__/guardrails.test.ts para comprobar que no rompió nada).
2. Corre npx eslint sobre cada archivo tocado. Corre NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "pos-display|pos/display|posService|CheckoutDialog|ConfiguracionPage|app/pos/page" y reporta SOLO errores en archivos de esta fase (los ~190 preexistentes en otros archivos no cuentan). tsc tarda varios minutos: espéralo.
3. Diseña y ejecuta casos: camino feliz, casos borde (carrito vacío, 200 líneas, cantidad 0, descuento mayor que el subtotal, impuesto incluido vs excluido, modificadores sin precio, terminal ajena, mensajes fuera de orden, enabled=false), y de error (JSON malformado, versión desconocida del protocolo).
4. Si faltan tests razonables, escríbelos tú en src/__tests__/pos-display/ (archivos .test.ts nuevos, no edites los del builder) y déjalos en el repo; lista sus rutas en testsAgregados.
5. Para la Parte C (UI): además del análisis estático, intenta una verificación de render real: si puedes levantar un servidor de Next sin colisionar con otro que ya esté corriendo en este directorio (revisa primero puertos en uso), abre /pos-display y comprueba que renderiza el estado Conectando/Reposo sin errores en consola; si no es viable, dilo en noProbado en vez de fingirlo.
${ALCANCE_CONGELADO_A}
Sé específico y verificable. Calificación de robustez de 1 a 10 con justificación breve en evidencia.`
}

function promptQA(parte, ronda, build, test) {
  const p = PARTES[parte]
  return `Eres el QA-REVIEWER de la parte ${p.nombre} de la Fase 0 del POS de doble pantalla (ronda ${ronda}). No construyes ni arreglas: auditas, calificas de 1 a 10 y das dirección concreta.
${CONTEXTO}
Alcance que debía cumplirse:
${p.alcance}
Informe del builder:
${JSON.stringify(build, null, 2)}
Informe del tester:
${JSON.stringify(test, null, 2)}
Rubric (5 dimensiones de 2 puntos, promedia): funcionalidad completa sin atajos; robustez (bordes, errores, concurrencia); consistencia con los patrones del proyecto (multi-tenant, capa de servicios, convenciones de organization_settings, i18n, moneda/zona horaria); resultados del tester (un solo fallo crítico limita el máximo a 6); trazabilidad (decisiones documentadas en el código).
Verifica tú mismo lo que afirman ambos leyendo el código: no te fíes de los informes. Nunca un 10 automático: solo si de verdad no encuentras nada. Entre 9,5 y 9,9 es aprobado, pero anota qué falta para el 10.
${ALCANCE_CONGELADO_A}
Si calificas < 9,5, cada problema debe traer una acción concreta y verificable para el builder (ejemplo bueno: 'projectCartForDisplay no suma extraPrice de modificadores al total de línea cuando quantity > 1: ver línea X'; ejemplo malo: 'mejorar la proyección').`
}

function formatearFeedback(test, qa) {
  const fallos = (test?.fallos || []).map((f, i) => `${i + 1}. [tester · ${f.severidad}] ${f.descripcion}\n   Reproducir: ${f.reproducir}\n   Esperado: ${f.esperado} | Obtenido: ${f.obtenido}`)
  const problemas = (qa?.problemas || []).map((p, i) => `${i + 1}. [qa · ${p.severidad}] ${p.descripcion}\n   Acción: ${p.accion}`)
  return [...problemas, ...fallos].join('\n')
}

async function cicloParte(parte, fase, feedbackInicial = null, maxRondas = MAX_RONDAS) {
  const rondas = []
  let feedback = feedbackInicial
  for (let ronda = 1; ronda <= maxRondas; ronda++) {
    log(`${PARTES[parte].nombre} — ronda ${ronda}`)
    const build = await agent(promptBuilder(parte, ronda, feedback), { label: `builder:${parte}:r${ronda + 3}`, phase: fase, schema: BUILD_SCHEMA })
    if (!build) { rondas.push({ ronda, error: 'builder sin resultado' }); break }
    const test = await agent(promptTester(parte, ronda, build), { label: `tester:${parte}:r${ronda + 3}`, phase: fase, schema: TEST_SCHEMA })
    const qa = await agent(promptQA(parte, ronda, build, test), { label: `qa:${parte}:r${ronda + 3}`, phase: fase, schema: QA_SCHEMA, effort: 'high' })
    rondas.push({ ronda, build, test, qa })
    const nota = qa?.calificacion ?? 0
    log(`${PARTES[parte].nombre} — ronda ${ronda}: QA ${nota}/10, tester ${test?.robustez ?? '?'}/10, veredicto ${qa?.veredicto ?? '?'}`)
    if (nota >= UMBRAL) break
    feedback = formatearFeedback(test, qa)
  }
  return { parte, rondas }
}

const FEEDBACK_A_INICIAL = `
CIERRE DEFINITIVO DE LA PARTE A. El orquestador (con autoridad del dueño) ha TOMADO estas decisiones de diseño: no las discutas, impleméntalas tal cual. El código actual está en verde (14+ suites, 230-377 tests) y ya incluye display_alive/display_bye, watchdog de instancia, ventana de adopción, releaseActiveInstance, lastByeAt/lastStaleAt e isEntry(): NO lo reescribas, solo aplica esta lista.

D1 TOTALES (prioridad máxima). DisplayCart.subtotal = Σ line.total (bruto, qty × unitPrice), siempre. El override de totales pasa a ser DisplayTotalsOverride = { discountTotal, taxTotal, total } SIN subtotal; documenta en la cabecera de projection.ts que la Parte B lo calcula con el MISMO motor que ve el cajero (calculateCartTaxes de src/lib/utils/taxCalculations.ts, que honra item.tax_excluded y el override de organization_taxes) y que sin override se usan los campos del cart (posService) tal cual. Añade a DisplayLine 'taxExcluded: boolean' y 'taxIncluded: boolean' (Boolean(item.tax_excluded) / Boolean(item.tax_included)). JSDoc de DisplayCart.taxIncluded: «true si ALGUNA línea lleva impuesto incluido (regla de calculateCartTotals); con carrito mixto la Parte C debe mirar las líneas».
D2 VARIANTES. DisplayLine.variant: Array<{ attr: string; value: string }> | null, desde product.variant_data (solo valores no vacíos, en orden de la BD). name sigue siendo product.name. Test con variante y sin variante.
D3 VERSIÓN INCOMPATIBLE. protocol.ts exporta readEnvelopeVersion(value): number | null. BroadcastChannelReceiver expone getters incompatibleVersionAt: number | null e incompatibleVersionCount: number, actualizados cuando llega un sobre con terminalId correcto y v ≠ PROTOCOL_VERSION (se descarta igual). JSDoc: la Parte C muestra «Actualice la pantalla» si incompatibleVersionAt es reciente y no hay mensajes válidos.
D4 terminalId VACÍO. Los constructores de BroadcastChannelTransport y BroadcastChannelReceiver lanzan Error('[pos-display] terminalId vacío') si terminalId no es string no vacío. Test.
D5 NORMALIZACIONES. discountTotal = Math.max(0, toAmount(cart.discount_total)) con comentario de una línea. projectCartForDisplay(cart: Cart | null | undefined, opts): sin cart devuelve un DisplayCart vacío (id '', lines [], totales 0, taxIncluded false) y el JSDoc dice que la Parte B debe emitir mode 'idle' con cart null en ese caso. opts.currency: si no es string no vacío, usar 'COP' y console.warn una vez.
D6 announce. Añadir a la interfaz DisplayTransport y a BroadcastChannelTransport 'announce(hello: Extract<DownMessageDraft, { t: 'hello' }>, state: DisplayState): void' que publica hello y luego state en la misma vuelta; JSDoc de publish(): «tras relevar a otra instancia, usar announce() para que el receptor no descarte el primer state». Test: state antes de hello se descarta; announce no.
D7 DOCUMENTACIÓN. terminal.ts cabecera: «pos_terminal_id es EXCLUSIVA de UUID RFC 4122 (generateTerminalId produce v4; se acepta cualquier versión para no invalidar ids guardados); F2 debe migrar (leer, registrar en pos_terminals, conservar) y nunca escribir otro formato; la clave es por perfil de navegador, no por organización». transport.ts, bloque de presencia: «Una pantalla por terminal en F0: con dos, el display_bye de una deja lastDisplaySeenAt en null hasta el siguiente display_alive (≤ 1 s)». BroadcastChannelTransportOptions.instanceId → renombrar a __testInstanceId y JSDoc «NUNCA persistir».
D8 TESTS. Fusionar TODAS las suites de src/__tests__/pos-display/ en cuatro por dominio: projection.test.ts, protocol.test.ts, transport.test.ts, terminal.test.ts. Conservar cada caso útil (sin duplicados), ELIMINAR los tests «HALLAZGO» que afirman comportamiento indeseado (conviértelos en tests del comportamiento decidido arriba) y borrar los archivos por ronda (builder-r*, tester-r*, qa-*). Resultado: 4 archivos, todo en verde.
D9 INFORME. En resumen pega literalmente: el comando de jest con Tests: N passed; el comando de eslint; y para tsc el comando exacto con NODE_OPTIONS, el exit code y el recuento de errores separando «en archivos de pos/display o pos-display» de «preexistentes». No escribas «0 errores» sin ese detalle.
`

phase('Parte A')
log('Parte A aprobada en v3 con 9,7: se salta.')
const resultadoA = { parte: 'A', rondas: [{ ronda: 0, build: { resumen: 'Aprobada en v3 (wf_af00c7fa-37d)', archivos: ['src/lib/pos/display/protocol.ts', 'src/lib/pos/display/projection.ts', 'src/lib/pos/display/transport.ts', 'src/lib/pos/display/terminal.ts', 'src/lib/pos/display/index.ts'], feedbackAtendido: [], decisiones: [], pendientes: [] }, test: null, qa: { calificacion: 9.7, veredicto: 'aprobado', fortalezas: [], problemas: [], paraElDiez: [] } }] }

phase('Partes B C D')
const resultadosBCD = await parallel(['B', 'C', 'D'].map((p) => () => cicloParte(p, 'Partes B C D', 'RONDA 4 (la ronda 3 de la v3 dejó esta lista; el código YA existe y está en verde: no reescribas, corrige): ' + FEEDBACK_R3[p], 2)))
const partes = [resultadoA, ...resultadosBCD.filter(Boolean)]
const noAprobadas = partes.filter((r) => (r.rondas[r.rondas.length - 1]?.qa?.calificacion ?? 0) < UMBRAL).map((r) => r.parte)
if (noAprobadas.length) {
  log(`Partes sin aprobar tras ${MAX_RONDAS} rondas: ${noAprobadas.join(', ')}. Se escala sin integración.`)
  return { fecha: DATE, detenidoEn: noAprobadas, partes }
}

phase('Integración')
const resumenPartes = partes.map((r) => ({ parte: r.parte, archivos: r.rondas[r.rondas.length - 1].build.archivos, decisiones: r.rondas[r.rondas.length - 1].build.decisiones }))
const testInt = await agent(`Eres el TESTER DE INTEGRACIÓN de la Fase 0 del POS de doble pantalla. Las cuatro partes ya pasaron QA por separado; ahora pruébalas JUNTAS.
${CONTEXTO}
Archivos y decisiones por parte:
${JSON.stringify(resumenPartes, null, 2)}
Casos obligatorios:
1. Extremo a extremo en Node: con el transporte real (BroadcastChannel) y el emitter real, simula que POSService muta un carrito (usa el mismo camino de código que el POS, no el emitter a mano si es posible) y comprueba que un receptor recibe hello + state con la proyección correcta; luego need_snapshot desde el receptor y respuesta completa; luego heartbeat.
2. Coalescencia real: N mutaciones rápidas → un solo state.
3. Interruptor maestro: enabled=false → nada se emite; refresh() con enabled=true → empieza a emitir.
4. Cobro: cash con recibido/cambio, card, qr → estados correctos; confirmar → thanks; 8 s → idle (usa fake timers).
5. Regresión: npx jest completo (reporta fallos preexistentes aparte de los nuevos, con nombre), npx eslint de todos los archivos tocados en la fase, y tsc filtrado a los archivos de la fase.
6. Render: si es viable sin colisionar con otro servidor, /pos-display en un navegador; si no, dilo.
Deja los tests de integración en src/__tests__/pos-display/integracion.test.ts. No arregles nada; reporta.`, { label: 'tester:integracion', phase: 'Integración', schema: TEST_SCHEMA })

const qaFinal = await agent(`Eres el QA-REVIEWER FINAL de la Fase 0 del POS de doble pantalla. Califica la FASE COMPLETA (no una parte) de 1 a 10 con el rubric de 5 dimensiones, verificando el código tú mismo.
${CONTEXTO}
Partes y sus últimas calificaciones: ${JSON.stringify(partes.map((r) => ({ parte: r.parte, rondas: r.rondas.length, qa: r.rondas[r.rondas.length - 1]?.qa?.calificacion })))}
Informe del tester de integración:
${JSON.stringify(testInt, null, 2)}
Criterios de aceptación de la fase (del plan §12 Fase 0): con dos ventanas en un portátil, teclear una venta se refleja en < 100 ms; cerrar la caja deja la pantalla en Conectando en ≤ 3 s; recargar la pantalla recupera la venta completa. Evalúa si el código construido los cumple o qué le falta. Anota explícitamente qué queda para el 10 y qué debería probarse en el hardware real (pantalla táctil) que no se pudo probar aquí.`, { label: 'qa:final', phase: 'Integración', schema: QA_SCHEMA, effort: 'high' })

return { fecha: DATE, partes, integracion: { test: testInt, qa: qaFinal } }